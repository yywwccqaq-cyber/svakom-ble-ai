# SL278K 加热能力验证（暂不启用控制）

## 当前结论

GATT 枚举只能确认厂商自定义入口：

- `FFE1`：Write Without Response
- `FFE2`：Read / Notify
- `AE01`：Write Without Response
- `AE02`：Notify

它不能证明某个字节代表摄氏温度，也不能证明设备固件的安全上限。现有逆向记录仅给出 `55 05 ...` 加热开关帧，仍未确认 `idx` 的含义、温度反馈格式和硬件过温保护，因此 MCP 暂不暴露加热写入。

## 只读采集

1. 完全退出 `bridge.py` 和手机官方 App，并关闭手机蓝牙。
2. 设备放在稳定、耐热表面，不要佩戴或使用；确保实体停止/关机键随手可按。
3. 更新并运行临时分支中的 `scan.py`。
4. 脚本只读取 GATT 值、描述符和通知，不会写 `FFE1/AE01`。
5. 按提示只操作设备实体加热键：开启一次、关闭一次。
6. 将生成的 `gatt_report.json` 提供给维护者；不要提供 `BRIDGE_SECRET`。

## 开放 MCP 前的必要证据

必须同时确认：

1. 实际写入的 characteristic UUID；
2. 开启、关闭和温度设定字节的确切语义；
3. 温度反馈来源、单位和异常值行为；
4. 厂商说明书规定的正常温度/上限；
5. 设备断开、Railway 失联和超时后都能自动关热；
6. 服务端与电脑端都拒绝超过硬编码上限的请求。

如果报告只出现 `55aa` 心跳或 ACK，而没有温度反馈，则仅凭 BLE GATT 不能安全实现“设定温度”；需要再抓取官方 App 的 BLE 写入/通知日志并核对官方说明书。
